# 开发日志 — 2026-06-06

## 当前阶段：Phase 0 — 项目骨架搭建

---

## 今日完成事项

### 项目规划与文档体系搭建

- [x] 撰写毕业设计文档（需求分析、数据库设计、系统设计）
- [x] 创建 AGENTS.md — Agent + Skill 调度总入口
- [x] 创建 6 个 Skill 定义文件（sql/springboot/vue3/miniprogram/api/test）
- [x] 创建 8 个 Phase Spec 文件（phase-00 ~ phase-07）
- [x] 创建数据库建表脚本 `docs/database/schema.sql`（8 张表）
- [x] 创建开发日志体系 `devlog/`
- [x] 创建开发计划 `docs/development-plan.md`
- [x] 创建/优化 CLAUDE.md 项目指引文件

### Phase 0 编码实现

- [x] **Step 0.1** — Spring Boot 项目骨架
  - `server/pom.xml`（Spring Boot 3.2.5 + MyBatis-Plus 3.5.6 + JWT 0.12.5）
  - `AutoPartsApplication.java` 启动类
  - `application.yml` + `application-dev.yml` + `application-prod.yml`
- [x] **Step 0.2** — 后端公共模块（com.autoparts.common）
  - `Result.java` — 统一响应体
  - `ResultCode.java` — 状态码枚举
  - `BusinessException.java` — 业务异常
  - `GlobalExceptionHandler.java` — 全局异常处理（含参数校验）
  - `JwtUtil.java` — JWT 工具（生成/解析/验证 Token）
  - `Constants.java` — 系统常量
- [x] **Step 0.3** — 后端配置类（com.autoparts.config）
  - `SecurityConfig.java` — Spring Security + JWT 过滤器 + 白名单
  - `CorsConfig.java` — 跨域配置
  - `MyBatisPlusConfig.java` — 分页插件
  - `JacksonConfig.java` — 日期格式 + Long→String 防精度丢失
- [x] **Step 0.4** — 健康检查接口
  - `HealthController.java` → `GET /api/v1/health`
- [x] **Step 0.5** — 数据库初始化
  - `docs/database/init-data.sql`（默认管理员 admin/admin123 + 示例商品数据）
- [x] **Step 0.6** — Vue3 管理后台骨架
  - `package.json`（Vue 3.4 + Element Plus 2.6 + Pinia + Axios）
  - `vite.config.js`（代理 /api → localhost:8080）
  - `main.js` + `App.vue` + `router/index.js` + `api/request.js` + `store/index.js`
- [x] **Step 0.7** — 登录页 + 主布局
  - `Login.vue`（用户名/密码表单 + 对接 API）
  - `MainLayout.vue`（侧边栏 + 顶栏 + 下拉菜单）
  - `Dashboard.vue`（首页占位）
- [x] **Step 0.8** — 微信小程序骨架
  - `app.js` + `app.json`（4 个 Tab 页）+ `app.wxss`
  - `project.config.json`
  - `utils/api.js`（请求封装）
  - `pages/index/` 首页 + 其余 3 页占位

---

## Phase 0 验收清单

| # | 检查项 | 状态 | 说明 |
|---|--------|------|------|
| 1 | `server/pom.xml` 依赖完整 | ✅ | Spring Boot 3.2.5 + MyBatis-Plus + JWT + Lombok + Hutool |
| 2 | `application.yml` 多环境配置 | ✅ | dev/prod 分离，数据库连接、JWT 密钥 |
| 3 | 后端公共模块 6 个类 | ✅ | Result, ResultCode, BusinessException, GlobalExceptionHandler, JwtUtil, Constants |
| 4 | 配置类 4 个 | ✅ | SecurityConfig(含JWT过滤器), CorsConfig, MyBatisPlusConfig, JacksonConfig |
| 5 | 健康检查接口 | ✅ | `GET /api/v1/health` → HealthController |
| 6 | 数据库脚本 | ✅ | schema.sql(8表) + init-data.sql(admin账号+示例数据) |
| 7 | Vue3 管理后台 | ✅ | package.json + vite + 路由 + Axios + Store + Login.vue |
| 8 | 微信小程序 | ✅ | app.js/json/wxss + 4个Tab页 + 请求封装 |

> **待实际验证：** 需要在本地环境执行 `mvn clean compile`、`npm install && npm run dev`、MySQL 建表来确认编译和运行正常。

---

## 问题与决策记录

| # | 问题/决策 | 结论 |
|---|-----------|------|
| 1 | 项目采用 AGENTS.md + Skill 驱动开发 | 每个 Phase 有独立 Spec，Agent 按规范执行 |
| 2 | Phase 0 拆分为 8 个小步骤 | 每步独立可验证，避免一步到位的风险 |
| 3 | SecurityConfig 内嵌 JwtAuthFilter | Phase 1 完善鉴权逻辑，Phase 0 仅搭框架 |
| 4 | 小程序 TabBar 图标暂用占位 | 需要准备 images/tab/ 下的图标文件 |

---

## 下一步

1. **本地验证 Phase 0** — `mvn clean compile`、`npm install && npm run dev`、MySQL 建表
2. **进入 Phase 1** — 用户认证与权限（JWT 登录、用户 CRUD）
